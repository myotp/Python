# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t deps/' to install these dependencies
# in `deps/` subdirectory.
#
# Note: The `deps` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10
